# Repository provenance

This repository was initialized as a fresh Zinuto Core source repository on
2026-08-11.

- Initial repository commit: `10131e1d63968ddf35940b3d242b7b26fe56e52b`
- Initial commit subject: `chore: initialize public Core source`
- Destination: `Zinuto-Official/zinuto-core`
- History policy: fresh repository history; this repository retains no parent
  history before its initial commit

The initial snapshot contains only the GPL-3.0-only local desktop source, its
local contracts, redistributable assets, tests, and build tooling. Official
accounts, payments, support, notices, presence, updater code, signing,
notarization, Store submission, deployment assets, production credentials, and
hosted-service implementation are excluded.

The initial commit is recorded for traceability. It does not create a build,
runtime, deployment, or release dependency on another repository.
