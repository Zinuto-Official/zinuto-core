Vendored `klinecharts@10.0.0-beta2` runtime consumed by the desktop app.

`manifest.json` is the sole provenance authority. It pins the upstream tag,
commit, source archive, npm integrity, input/output hashes, toolchain, patch,
behavior requirement, and license. Rebuild with `npm run build:klinecharts-vendor`
and verify checked-in bytes with `npm run check:klinecharts-vendor`.
